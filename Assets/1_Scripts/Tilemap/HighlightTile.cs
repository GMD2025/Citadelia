using UnityEngine;

namespace _1_Scripts.Tilemap
{
    public class HighlightTile : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
